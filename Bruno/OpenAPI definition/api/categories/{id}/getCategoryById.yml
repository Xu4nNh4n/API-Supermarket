info:
  name: getCategoryById
  type: http
  seq: 1
  tags:
    - category-controller

http:
  method: GET
  url: "{{baseUrl}}/api/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/categories/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "categoryId": 0,
            "categoryName": "",
            "description": "",
            "isActive": false,
            "createAt": ""
          }
