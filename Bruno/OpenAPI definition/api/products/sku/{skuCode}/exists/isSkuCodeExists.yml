info:
  name: isSkuCodeExists
  type: http
  seq: 1
  tags:
    - product-controller

http:
  method: GET
  url: "{{baseUrl}}/api/products/sku/:skuCode/exists"
  params:
    - name: skuCode
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/products/sku/:skuCode/exists"
      method: GET
      params:
        - name: skuCode
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "false"
