info:
  name: exception-message
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/test/logging/exception-message
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
