info:
  name: getSuppliersByIsActiveTrue
  type: http
  seq: 1
  tags:
    - supplier-controller

http:
  method: GET
  url: "{{baseUrl}}/api/suppliers/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/suppliers/active"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "supplierId": 0,
              "supplierName": "",
              "supplierPhone": "",
              "supplierEmail": "",
              "supplierAddress": "",
              "isActive": false,
              "createAt": ""
            }
          ]
