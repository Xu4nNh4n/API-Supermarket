info:
  name: getAllRoles
  type: http
  seq: 1
  tags:
    - role-controller

http:
  method: GET
  url: "{{baseUrl}}/api/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/roles"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "roleId": 0,
              "roleName": "",
              "description": "",
              "createAt": ""
            }
          ]
