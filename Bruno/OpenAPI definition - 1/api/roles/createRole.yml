info:
  name: createRole
  type: http
  seq: 2
  tags:
    - role-controller

http:
  method: POST
  url: "{{baseUrl}}/api/roles"
  body:
    type: json
    data: |-
      {
        "roleName": "",
        "description": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/roles"
      method: POST
      body:
        type: json
        data: |-
          {
            "roleName": "",
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "roleId": 0,
            "roleName": "",
            "description": "",
            "createAt": ""
          }
